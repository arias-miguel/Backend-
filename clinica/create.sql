DROP TABLE IF EXISTS ODONTOLOGO;
CREATE TABLE IF NOT EXISTS ODONTOLOGO (
    id int numeromatricula primary key,
    nombre varchar(255),
    apellido varchar (255));

-- Pueden agregar acá sentencias Insert para precargar datos.